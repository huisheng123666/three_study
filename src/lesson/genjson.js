// import config from "../config";
//
// let list = config.split('xxxx').filter(item => item && item !== '\n')
//
// list = list.map(item => item.replace(/\t/g, '').replace('\n', ''))
//
// console.log(JSON.stringify(list, null, 2))
